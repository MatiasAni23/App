const scrollKey='contratos-scroll-position';const workflowKey='contratos-workflow-step';const shell=document.querySelector('.workflow-shell');const markers=[...document.querySelectorAll('[data-step-indicator]')];function setWorkflowStep(step,{scroll=true}={}){const safeStep=Math.min(3,Math.max(1,Number(step)||1));if(!shell)return;shell.dataset.activeStep=String(safeStep);markers.forEach(marker=>{const markerStep=Number(marker.dataset.stepIndicator);marker.classList.toggle('active',markerStep===safeStep);marker.classList.toggle('complete',markerStep<safeStep);marker.querySelector('.step-marker').textContent=markerStep<safeStep?'✓':String(markerStep)});try{sessionStorage.setItem(workflowKey,String(safeStep))}catch(error){}if(scroll){history.replaceState(null,'',`#paso-${safeStep}`);window.scrollTo({top:0,behavior:'smooth'})}}window.addEventListener('DOMContentLoaded',()=>{try{const savedPosition=sessionStorage.getItem(scrollKey);if(savedPosition!==null){sessionStorage.removeItem(scrollKey);window.requestAnimationFrame(()=>window.scrollTo(0,Number(savedPosition)))}}catch(error){}let initialStep=Number(document.body.dataset.workflowStep)||1;try{initialStep=Number(sessionStorage.getItem(workflowKey))||initialStep}catch(error){}setWorkflowStep(initialStep,{scroll:false})});document.querySelectorAll('[data-next-step]').forEach(button=>button.addEventListener('click',()=>setWorkflowStep(button.dataset.nextStep)));document.querySelectorAll('form').forEach(form=>form.addEventListener('submit',()=>{try{sessionStorage.setItem(scrollKey,String(window.scrollY))}catch(error){}}));const generationForm=document.getElementById('generate-contract-form');if(generationForm)generationForm.addEventListener('submit',function(){try{sessionStorage.setItem(workflowKey,'2')}catch(error){}const loadingModal=document.getElementById('loading-modal');const steps=[...loadingModal.querySelectorAll('.loading-step')];loadingModal.hidden=false;this.querySelector('button[type="submit"]').disabled=true;let current=0;const update=()=>{steps.forEach((step,index)=>step.classList.toggle('done',index<current));steps.forEach((step,index)=>step.classList.toggle('active',index===current));if(current<steps.length-1){current+=1;window.setTimeout(update,900)}};update()});
